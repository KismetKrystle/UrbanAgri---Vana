import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // You can skip this if you don't have an index.css file
import AppComponent from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
